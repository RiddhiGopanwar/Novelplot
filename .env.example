# Optional but recommended. The Discover page works without this — the code
# also passes a country param to work around Google's occasional regional
# empty-results quirk. But if searches are still unreliable for you, or you
# hit rate limits, adding a real key is the most reliable fix.
#
# Get a key: https://console.cloud.google.com/apis/credentials
# (enable the "Books API" for your project first)
NEXT_PUBLIC_GOOGLE_BOOKS_API_KEY=AIzaSyBkQdu6A097SARoQN_nFJFdy4upcPtr95c

# Required for Sign Up / Log In / Logout and per-user data.
# 1. Create a free project at https://supabase.com
# 2. In Project Settings > API, copy the "Project URL" and "anon public" key
# 3. In the SQL Editor, run supabase/schema.sql from this project once
NEXT_PUBLIC_SUPABASE_URL=https://yowdshemjxbbpinwlvop.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_lghmrHFhn8_Qg0DfVPdrgA_DthDAuSx
